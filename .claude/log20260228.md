# 2026-02-28 開発ログ

## クリップボードペースト機能追加
- `ServerSettingsView.swift` のServer URLおよびAPI Key入力欄にクリップボードペーストボタンを追加
- `UIPasteboard.general.string` を使用してクリップボードの内容を取得
- `UIKit` の import を追加（`UIPasteboard` 使用のため）
- ボタンアイコン: `doc.on.clipboard` (SF Symbols)

## GPU使用中エラーメッセージのUI表示

### 変更ファイル
1. **HILServerClient.swift** — `validateResponse` でレスポンスボディから `message`/`error` フィールドを取得。`HILError.serverError` に `message` パラメータ追加。409 の既知パターンを日本語にマッピング（"GPU使用中", "トレーニング中"）
2. **HILViewModel.swift** — `startTraining` のエラーメッセージを短縮。`autoClearError()` で5秒後に自動消去。`syncModel` のパターンマッチを `message` パラメータに対応
3. **RightPanelView.swift** — `hilErrorMessage` プロパティ追加。TrainingStatusView の下にエラーメッセージ表示（赤背景）
4. **MainView.swift** — `hilErrorMessage: hilViewModel.errorMessage` を RightPanelView に渡す
